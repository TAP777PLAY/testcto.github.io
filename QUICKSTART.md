# 🚀 Быстрый старт SiteBuilder

## За 5 минут до первого запуска

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка базы данных

#### Вариант А: Локальная PostgreSQL

Установите PostgreSQL и создайте базу:

```bash
createdb sitebuilder
```

Обновите `.env`:
```env
DATABASE_URL="postgresql://localhost:5432/sitebuilder?schema=public"
```

#### Вариант Б: Docker

```bash
docker run --name sitebuilder-postgres -e POSTGRES_PASSWORD=password -e POSTGRES_DB=sitebuilder -p 5432:5432 -d postgres
```

### 3. Инициализация базы данных

```bash
npx prisma migrate dev --name init
```

### 4. Запуск приложения

```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000)

## 🎯 Первые шаги

### 1. Регистрация

1. Перейдите на главную страницу
2. Нажмите "Регистрация"
3. Заполните форму
4. Войдите в систему

### 2. Создание сайта

1. В дашборде нажмите "+ Создать сайт"
2. Укажите название и slug
3. Нажмите "Создать"

### 3. Редактирование

1. Нажмите "Редактировать" на карточке сайта
2. Добавляйте блоки через кнопки сверху
3. Редактируйте блоки кнопкой ✏️
4. Перетаскивайте блоки для изменения порядка
5. Удаляйте ненужные блоки кнопкой 🗑️

### 4. Публикация

1. Нажмите "Опубликовать" в редакторе
2. Ваша страница опубликована!

## 🌐 Деплой на Vercel

### Самый быстрый способ

```bash
# Установите Vercel CLI
npm install -g vercel

# Залогиньтесь
vercel login

# Деплой
vercel
```

### Настройка переменных окружения

```bash
# Создайте Vercel Postgres
vercel postgres create

# Добавьте секреты
vercel env add NEXTAUTH_SECRET production
# Введите: $(openssl rand -base64 32)

vercel env add NEXTAUTH_URL production
# Введите: https://ваш-домен.vercel.app
```

### Production деплой

```bash
vercel --prod
```

## 📝 Полезные команды

```bash
# Разработка
npm run dev              # Запуск в dev режиме
npm run build            # Сборка production
npm run start            # Запуск production сервера

# База данных
npx prisma studio        # GUI для БД
npx prisma migrate dev   # Создать миграцию
npx prisma generate      # Сгенерировать клиент
npx prisma db push       # Быстрое обновление БД (dev)

# Vercel
vercel                   # Деплой preview
vercel --prod            # Деплой production
vercel logs              # Просмотр логов
vercel env ls            # Список переменных
```

## 🐛 Решение проблем

### Ошибка подключения к БД

```bash
# Проверьте что PostgreSQL запущен
sudo service postgresql status

# Или Docker
docker ps | grep postgres
```

### Ошибка Prisma Client

```bash
# Пересоздайте клиент
rm -rf node_modules/@prisma/client
npx prisma generate
```

### Ошибка NextAuth

```bash
# Убедитесь что NEXTAUTH_SECRET установлен
echo $NEXTAUTH_SECRET

# Или сгенерируйте новый
openssl rand -base64 32
```

## 📚 Дальнейшее изучение

- [README.md](./README.md) - Полная документация
- [ARCHITECTURE.md](./ARCHITECTURE.md) - Архитектура проекта
- [VERCEL_DEPLOYMENT.md](./VERCEL_DEPLOYMENT.md) - Детальный гайд по Vercel

## 💡 Примеры использования

### Создать блок заголовка

```javascript
// POST /api/pages/:pageId/blocks
{
  "type": "heading",
  "content": {
    "text": "Добро пожаловать!",
    "level": "h1"
  },
  "order": 0
}
```

### Создать блок кнопки

```javascript
// POST /api/pages/:pageId/blocks
{
  "type": "button",
  "content": {
    "text": "Узнать больше",
    "link": "https://example.com",
    "style": "primary"
  },
  "order": 1
}
```

## 🎨 Настройка темы

```javascript
// PUT /api/sites/:siteId/theme
{
  "primaryColor": "#3B82F6",
  "secondaryColor": "#10B981",
  "backgroundColor": "#FFFFFF",
  "textColor": "#1F2937",
  "fontFamily": "Inter"
}
```

## ✨ Готово!

Теперь вы можете создавать сайты с помощью SiteBuilder!

Если возникли вопросы - создавайте Issues в репозитории.

---

Удачи в создании сайтов! 🚀
