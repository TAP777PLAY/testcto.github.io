# Админ-панель и управление контентом (Фаза 5)

## Обзор

В проекте реализована полнофункциональная админ-панель для управления сайтом, пользователями и контентом.

## Основные функции

### 1. Dashboard администратора (`/admin`)

Главная страница админ-панели с обзором основных метрик:
- Общая статистика (пользователи, сайты, страницы)
- Графики активности по дням
- Топ страниц по просмотрам
- Последняя активность пользователей
- Быстрые действия

**Доступ:** только для пользователей с ролью `admin`

### 2. Управление пользователями (`/admin/users`)

Функции:
- Просмотр всех пользователей
- Поиск по имени или email
- Фильтрация по роли (admin/user)
- Изменение роли пользователя
- Удаление пользователей
- Просмотр количества сайтов у каждого пользователя

### 3. Управление страницами (`/admin/pages`)

Функции:
- Просмотр всех страниц на всех сайтах
- Поиск по названию или slug
- Фильтрация по статусу публикации
- Просмотр владельца страницы
- Статистика по блокам и просмотрам

### 4. Медиа библиотека (`/admin/media`)

Функции:
- Просмотр всех загруженных медиа файлов
- Фильтрация по типу (изображения, видео, документы)
- Информация о размере файла
- Дата загрузки

### 5. Аналитика (`/admin/analytics`)

Функции:
- Обзор ключевых метрик
- Графики просмотров по дням
- График активности по дням
- Топ 10 самых просматриваемых страниц
- Выбор периода (24ч, 7д, 30д, 90д)

### 6. Логи активности (`/admin/logs`)

Функции:
- Просмотр всех действий пользователей
- Фильтрация по типу действия (create, update, delete, publish, unpublish)
- Фильтрация по типу сущности (site, page, block, user, settings)
- Просмотр деталей каждого действия
- Временная метка каждого действия

### 7. Настройки сайта (`/sites/[siteId]/settings`)

Функции:
- **Основные настройки:**
  - Название сайта
  - Описание
  - Favicon URL
  - Logo URL

- **SEO и метаданные:**
  - Meta Title
  - Meta Description
  - Meta Keywords
  - Open Graph Image
  - Google Analytics ID
  - Facebook Pixel ID
  - Custom CSS
  - Custom JavaScript

### 8. Система уведомлений

Функции:
- Колокольчик уведомлений в header
- Отображение непрочитанных уведомлений
- Автоматическое обновление каждую минуту
- Отметка уведомлений как прочитанных
- Удаление уведомлений

## API Endpoints

### Админ API

#### Пользователи
- `GET /api/admin/users` - Список пользователей с пагинацией и фильтрами
- `PATCH /api/admin/users/[userId]` - Изменение роли пользователя
- `DELETE /api/admin/users/[userId]` - Удаление пользователя

#### Аналитика
- `GET /api/admin/analytics?period=7d` - Получение аналитических данных

#### Логи
- `GET /api/admin/logs` - Получение логов активности с фильтрами

#### Страницы
- `GET /api/admin/pages` - Список всех страниц с фильтрами

#### Медиа
- `GET /api/admin/media` - Список медиа файлов

#### Поиск
- `GET /api/admin/search?q=query&type=all` - Глобальный поиск по контенту

### Настройки сайта
- `GET /api/sites/[siteId]/settings` - Получение настроек сайта
- `PUT /api/sites/[siteId]/settings` - Обновление настроек сайта

### Уведомления
- `GET /api/notifications` - Получение уведомлений текущего пользователя
- `PATCH /api/notifications/[notificationId]` - Отметить как прочитанное
- `DELETE /api/notifications/[notificationId]` - Удалить уведомление

### Аналитика просмотров
- `POST /api/pageviews` - Трекинг просмотра страницы

## База данных

### Новые модели

#### SiteSettings
```prisma
model SiteSettings {
  id              String   @id @default(cuid())
  siteId          String   @unique
  site            Site     @relation(fields: [siteId], references: [id], onDelete: Cascade)
  metaTitle       String?
  metaDescription String?
  metaKeywords    String?
  ogImage         String?
  googleAnalytics String?
  facebookPixel   String?
  customCSS       String?  @db.Text
  customJS        String?  @db.Text
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt
}
```

#### Media
```prisma
model Media {
  id        String   @id @default(cuid())
  name      String
  url       String
  type      String
  size      Int
  userId    String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  @@index([userId])
}
```

#### PageView
```prisma
model PageView {
  id        String   @id @default(cuid())
  pageId    String
  page      Page     @relation(fields: [pageId], references: [id], onDelete: Cascade)
  ipAddress String?
  userAgent String?
  referrer  String?
  createdAt DateTime @default(now())
  @@index([pageId])
  @@index([createdAt])
}
```

#### ActivityLog
```prisma
model ActivityLog {
  id        String   @id @default(cuid())
  userId    String
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  action    String
  entity    String
  entityId  String?
  details   Json?
  ipAddress String?
  createdAt DateTime @default(now())
  @@index([userId])
  @@index([createdAt])
  @@index([entity, entityId])
}
```

#### Notification
```prisma
model Notification {
  id        String   @id @default(cuid())
  userId    String
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  title     String
  message   String
  type      String
  read      Boolean  @default(false)
  link      String?
  createdAt DateTime @default(now())
  @@index([userId, read])
  @@index([createdAt])
}
```

## Утилиты

### Activity Logger (`/lib/activityLogger.ts`)

Функции для логирования активности:
```typescript
logActivity({
  userId: string,
  action: 'create' | 'update' | 'delete' | 'publish' | 'unpublish',
  entity: 'site' | 'page' | 'block' | 'user' | 'settings',
  entityId?: string,
  details?: Record<string, any>,
  ipAddress?: string,
})
```

Создание уведомлений:
```typescript
createNotification({
  userId: string,
  title: string,
  message: string,
  type: string,
  link?: string,
})
```

### Admin Auth (`/lib/adminAuth.ts`)

Middleware для проверки прав администратора:
```typescript
const auth = await requireAdmin();
if (!auth.authorized) {
  return auth.response; // 401/403
}
// auth.userId - ID текущего пользователя
```

Получение текущей сессии:
```typescript
const auth = await getSession();
if (!auth.authorized) {
  return auth.response; // 401
}
// auth.userId, auth.userRole
```

## Компоненты

### NotificationBell (`/components/NotificationBell.tsx`)

Компонент колокольчика уведомлений:
- Отображает количество непрочитанных
- Dropdown с последними уведомлениями
- Автоматическое обновление
- Действия: прочитать, удалить

Использование:
```tsx
import NotificationBell from '@/components/NotificationBell';

<NotificationBell />
```

## Навигация

### Доступ к админ-панели

1. Пользователи с ролью `admin` видят ссылку "🔧 Admin Panel" в header dashboard
2. Прямой доступ: `/admin`
3. Все маршруты админ-панели защищены проверкой роли

### Меню админ-панели

- 📊 Dashboard - `/admin`
- 👥 Пользователи - `/admin/users`
- 📄 Страницы - `/admin/pages`
- 🖼️ Медиа - `/admin/media`
- 📈 Аналитика - `/admin/analytics`
- 📝 Логи - `/admin/logs`

## Безопасность

1. **Проверка ролей:** Все админ-маршруты проверяют роль пользователя
2. **Логирование:** Все критичные действия записываются в ActivityLog
3. **Защита API:** Все API endpoints требуют аутентификацию
4. **Изоляция данных:** Обычные пользователи видят только свои данные
5. **Каскадное удаление:** Связанные данные удаляются автоматически

## Использование

### Как сделать пользователя администратором

1. Через базу данных:
```sql
UPDATE "User" SET role = 'admin' WHERE email = 'admin@example.com';
```

2. Через Prisma Studio:
```bash
npx prisma studio
```
Откройте модель User и измените поле role на "admin"

3. Через админ-панель (если уже есть admin):
- Перейдите в `/admin/users`
- Найдите пользователя
- Измените роль через dropdown

### Как добавить новое уведомление

```typescript
import { createNotification } from '@/lib/activityLogger';

await createNotification({
  userId: 'user_id',
  title: 'Заголовок',
  message: 'Сообщение',
  type: 'info', // или 'success', 'warning', 'error'
  link: '/path/to/resource', // опционально
});
```

### Как отследить просмотр страницы

На клиенте:
```typescript
fetch('/api/pageviews', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ pageId: 'page_id' }),
});
```

## Будущие улучшения

- [ ] Экспорт аналитики в CSV/Excel
- [ ] Расширенные графики и визуализации
- [ ] Real-time уведомления через WebSockets
- [ ] Bulk операции (массовое удаление, изменение)
- [ ] Расширенный поиск с автодополнением
- [ ] Роли и права доступа (RBAC)
- [ ] Backup и восстановление данных
- [ ] API Rate Limiting
- [ ] Email уведомления

## Поддержка

Для вопросов и предложений создавайте issues в репозитории проекта.
