# 📐 Page Builder - Визуальный редактор

Полное руководство по использованию визуального редактора страниц SiteBuilder.

## 🎯 Обзор

Page Builder - это мощный визуальный редактор для создания и редактирования страниц сайта. Он предоставляет интуитивный интерфейс с drag-and-drop функциональностью, системой undo/redo и live preview.

## 🏗️ Архитектура

### Основные компоненты

```
PageBuilder/
├── PageBuilder.tsx      # Главный компонент редактора
├── Toolbar.tsx          # Панель инструментов
├── BlockPanel.tsx       # Панель выбора блоков
├── Canvas.tsx           # Холст для сборки страницы
├── SettingsPanel.tsx    # Панель настроек блока
└── BlockEditors.tsx     # Редакторы для разных типов блоков
```

### Структура интерфейса

```
┌─────────────────────────────────────────────────────┐
│                    Toolbar                          │
├──────────┬──────────────────────────┬───────────────┤
│          │                          │               │
│  Block   │         Canvas           │   Settings    │
│  Panel   │      (Drag & Drop)       │     Panel     │
│          │                          │               │
└──────────┴──────────────────────────┴───────────────┘
```

## 🎨 Панель блоков (Block Panel)

### Категории блоков

- **Текст** - Текстовые элементы
- **Медиа** - Изображения и галереи
- **Элементы** - Кнопки, формы, разделители

### Поиск блоков

Используйте поле поиска для быстрого нахождения нужного типа блока.

### Добавление блока

1. Выберите категорию или используйте поиск
2. Нажмите на нужный тип блока
3. Блок автоматически добавится в конец страницы

## 🖼️ Canvas (Холст)

### Режимы работы

#### Режим редактирования
- Drag & Drop для изменения порядка блоков
- Клик на блок для выбора
- Наведение для отображения кнопок управления

#### Режим предпросмотра (Live Preview)
- Просмотр страницы в финальном виде
- Скрытие элементов управления
- Реалистичное отображение контента

### Управление блоками

Каждый блок имеет три кнопки:
- **⋮⋮** - Перетащить блок (Drag handle)
- **✏️** - Редактировать содержимое
- **🗑️** - Удалить блок (с подтверждением)

## ⚙️ Панель настроек (Settings Panel)

Появляется при выборе блока. Позволяет настроить:

### Общие настройки

- **Цвет фона** - HEX или color picker
- **Цвет текста** - HEX или color picker
- **Выравнивание** - left, center, right, justify
- **Padding** - Внутренние отступы (0-100px)
- **Margin** - Внешние отступы (0-100px)
- **Border Radius** - Скругление углов (0-50px)

### Текстовые блоки (Heading, Text)

- **Размер шрифта** - 12px - 48px
- **Толщина шрифта** - 300, 400, 500, 600, 700

## 🧩 Типы блоков

### 1. Заголовок (Heading)

**Поля:**
- Текст заголовка
- Уровень (H1-H6)

**Использование:**
- Главные заголовки страниц (H1)
- Заголовки секций (H2, H3)
- Подзаголовки (H4-H6)

### 2. Текст (Text)

**Поля:**
- Многострочный текст

**Использование:**
- Параграфы текста
- Описания
- Длинный контент

### 3. Изображение (Image)

**Поля:**
- URL изображения
- Альтернативный текст (alt)
- Ширина (в пикселях или процентах)

**Рекомендации:**
- Используйте alt для доступности
- Указывайте ширину для контроля размера
- URL должен быть доступен публично

### 4. Галерея (Gallery)

**Поля:**
- Список изображений (URL + alt)
- Количество колонок (1-4)

**Функции:**
- Добавление изображений
- Удаление изображений
- Автоматическая сетка

**Использование:**
- Портфолио
- Фотоальбомы
- Каталоги продуктов

### 5. Кнопка (Button)

**Поля:**
- Текст кнопки
- Ссылка (URL)
- Стиль (primary, secondary, outline)

**Стили:**
- Primary - Синяя, основная
- Secondary - Серая, вторичная
- Outline - Контурная, прозрачная

### 6. Форма (Form)

**Поля:**
- Название формы
- Список полей
- Текст кнопки отправки

**Типы полей:**
- Text - Однострочный текст
- Email - Email адрес
- Tel - Телефон
- Textarea - Многострочный текст
- Select - Выпадающий список

**Настройка полей:**
- Label - Название поля
- Placeholder - Подсказка
- Required - Обязательное поле

**Использование:**
- Контактные формы
- Формы подписки
- Опросы

### 7. Разделитель (Divider)

**Поля:**
- Толщина линии (в пикселях)
- Стиль (solid, dashed, dotted)

**Использование:**
- Визуальное разделение секций
- Улучшение читаемости

### 8. Отступ (Spacer)

**Поля:**
- Высота отступа (в пикселях)

**Использование:**
- Вертикальные промежутки
- Контроль расстояния между блоками

## ⌨️ Горячие клавиши

### Основные

- `Ctrl+Z` / `Cmd+Z` - Отменить последнее действие
- `Ctrl+Y` / `Cmd+Shift+Z` - Повторить отменённое действие
- `Ctrl+S` / `Cmd+S` - Сохранить изменения

### Навигация

- `Click` - Выбрать блок
- `Drag` - Переместить блок

## 💾 Сохранение и синхронизация

### Автосохранение

- Срабатывает через 1 секунду после изменения
- Автоматическая синхронизация с сервером
- Индикация процесса сохранения

### Ручное сохранение

- Кнопка "Сохранить" в Toolbar
- Горячая клавиша `Ctrl+S`

### История изменений

- Хранится до 50 состояний
- Локально в памяти браузера
- Сбрасывается при перезагрузке страницы

## 🔄 Undo/Redo система

### Как работает

1. Каждое изменение добавляется в историю
2. Сохраняется полное состояние блоков
3. Можно вернуться к любому из 50 предыдущих состояний

### Действия, добавляемые в историю

- Добавление блока
- Удаление блока
- Изменение порядка блоков (drag & drop)
- Изменение содержимого блока
- Изменение стилей блока

### Индикаторы

- Кнопки Undo/Redo становятся активными, когда доступны
- Серые кнопки означают недоступность действия

## 👁️ Live Preview

### Режимы

#### Режим редактирования (по умолчанию)
- Видны элементы управления
- Можно редактировать блоки
- Drag & Drop активен

#### Режим просмотра (Preview)
- Скрыты элементы управления
- Отображается финальный вид страницы
- Нельзя редактировать

### Переключение режимов

- Кнопка "Просмотр" в Toolbar
- Иконка меняется в зависимости от режима
- Мгновенное переключение

## 🎨 Система стилизации

### JSON схема стилей

Каждый блок хранит стили в поле `content.styles`:

```typescript
{
  backgroundColor?: string;
  color?: string;
  textAlign?: 'left' | 'center' | 'right' | 'justify';
  padding?: string;
  margin?: string;
  borderRadius?: string;
  fontSize?: string;
  fontWeight?: string;
}
```

### Применение стилей

Стили применяются через inline-стили React для максимальной гибкости.

### CSS единицы

- Цвета: HEX (#ffffff) или rgb/rgba
- Размеры: px, %, em, rem
- По умолчанию используются пиксели

## 🗄️ Структура данных

### Блок (Block)

```typescript
{
  id: string;           // Уникальный ID (cuid)
  type: string;         // Тип блока
  content: {            // Содержимое блока
    // ... специфичные для типа поля
    styles?: {          // Опциональные стили
      backgroundColor?: string;
      color?: string;
      // ... другие стили
    }
  };
  order: number;        // Порядок блока
}
```

### Примеры

#### Заголовок
```json
{
  "id": "clx123abc",
  "type": "heading",
  "content": {
    "text": "Добро пожаловать",
    "level": "h1",
    "styles": {
      "color": "#1f2937",
      "textAlign": "center",
      "padding": "32px"
    }
  },
  "order": 0
}
```

#### Форма
```json
{
  "id": "clx456def",
  "type": "form",
  "content": {
    "title": "Свяжитесь с нами",
    "fields": [
      {
        "label": "Имя",
        "type": "text",
        "placeholder": "Введите ваше имя",
        "required": true
      },
      {
        "label": "Email",
        "type": "email",
        "placeholder": "example@email.com",
        "required": true
      }
    ],
    "submitText": "Отправить",
    "styles": {
      "backgroundColor": "#f9fafb",
      "padding": "24px",
      "borderRadius": "8px"
    }
  },
  "order": 1
}
```

## 🔧 Расширение редактора

### Добавление нового типа блока

1. **Создайте редактор блока** в `BlockEditors.tsx`
2. **Добавьте рендеринг** в `BlockRenderer.tsx`
3. **Добавьте в BlockPanel** с иконкой и категорией
4. **Определите default content** в editor page
5. **Добавьте case** в SortableBlock editor switch

### Пример: Новый блок "Video"

```typescript
// 1. BlockEditors.tsx
export function VideoEditor({ block, onUpdate, onStopEditing }: BlockEditorProps) {
  const [url, setUrl] = useState(block.content.url || '');
  
  const handleSave = () => {
    onUpdate(block.id, { ...block.content, url });
    onStopEditing();
  };

  return (
    <div className="space-y-3">
      <input
        value={url}
        onChange={(e) => setUrl(e.target.value)}
        placeholder="URL видео"
        className="w-full px-3 py-2 border border-gray-300 rounded"
      />
      <EditorButtons onSave={handleSave} onCancel={onStopEditing} />
    </div>
  );
}

// 2. BlockRenderer.tsx
case 'video':
  return (
    <div className="aspect-video" style={blockStyle}>
      <iframe
        src={block.content.url}
        className="w-full h-full"
        allowFullScreen
      />
    </div>
  );

// 3. BlockPanel.tsx
{ type: 'video', name: 'Видео', icon: '🎥', category: 'Медиа' }

// 4. editor page
video: { url: '', styles: {} }
```

## 📊 Best Practices

### Производительность

- Используйте debounce для автосохранения
- Ограничивайте историю (50 состояний)
- Оптимизируйте рендеринг больших списков

### UX

- Всегда показывайте индикацию сохранения
- Подтверждайте удаление блоков
- Используйте понятные иконки
- Предоставляйте визуальную обратную связь

### Данные

- Валидируйте на клиенте и сервере
- Используйте оптимистичные обновления UI
- Храните минимум данных в состоянии
- Синхронизируйте с сервером регулярно

### Доступность

- Добавляйте alt к изображениям
- Используйте семантические HTML теги
- Поддерживайте клавиатурную навигацию
- Обеспечивайте контрастность текста

## 🐛 Troubleshooting

### Блоки не сохраняются

- Проверьте консоль на ошибки API
- Убедитесь, что пользователь авторизован
- Проверьте права доступа к сайту

### Undo/Redo не работает

- Перезагрузите страницу
- Проверьте, что действия добавляются в историю
- История сбрасывается при перезагрузке

### Стили не применяются

- Проверьте синтаксис CSS значений
- Используйте валидные единицы измерения
- Убедитесь, что поле `styles` существует

### Drag & Drop не работает

- Убедитесь, что не в режиме preview
- Проверьте, что блоки имеют уникальные ID
- Перезагрузите страницу

## 🎓 Обучающие материалы

### Видео уроки (планируется)

1. Основы работы с редактором
2. Создание лендинга с нуля
3. Работа с формами и галереями
4. Продвинутая стилизация

### Примеры использования

См. файл [EXAMPLES.md](EXAMPLES.md) для готовых примеров.

## 📞 Поддержка

Если у вас возникли вопросы:

1. Проверьте эту документацию
2. Посмотрите примеры в EXAMPLES.md
3. Создайте Issue на GitHub
4. Обратитесь в сообщество

---

**Версия документации:** 0.2.0  
**Дата обновления:** 2024-11-02
