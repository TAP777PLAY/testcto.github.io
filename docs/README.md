# 📚 Документация SiteBuilder

Это сайт документации SiteBuilder, построенный на [Docusaurus](https://docusaurus.io/).

## 🚀 Быстрый старт

### Установка зависимостей

```bash
npm install
```

### Локальная разработка

```bash
npm start
```

Эта команда запустит локальный сервер разработки и откроет браузер. Большинство изменений отображаются в реальном времени без перезапуска сервера.

### Сборка

```bash
npm run build
```

Эта команда генерирует статические файлы в директорию `build`, которые можно развернуть на любом статическом хостинге.

### Деплой

#### На Vercel

```bash
npm install -g vercel
vercel
```

#### На GitHub Pages

```bash
npm run deploy
```

## 📁 Структура

```
docs/
├── docs/                    # Документация
│   ├── intro.md            # Главная страница
│   ├── quickstart.md       # Быстрый старт
│   ├── user-guide.md       # Руководство пользователя
│   ├── api.md              # API документация
│   ├── architecture.md     # Архитектура
│   └── examples.md         # Примеры
├── blog/                    # Блог
├── src/                     # Кастомные компоненты
│   ├── components/
│   ├── css/
│   └── pages/
├── static/                  # Статические файлы
│   └── img/
├── docusaurus.config.ts     # Конфигурация
└── sidebars.ts             # Боковая панель навигации
```

## ✍️ Добавление документации

### Создание новой страницы

1. Создайте файл в `docs/`:

```markdown
---
sidebar_position: 3
---

# Заголовок страницы

Содержание...
```

2. Добавьте страницу в `sidebars.ts`:

```typescript
tutorialSidebar: [
  'intro',
  'your-new-page',  // Добавьте здесь
],
```

### Создание категории

В `sidebars.ts`:

```typescript
{
  type: 'category',
  label: 'Название категории',
  items: ['page1', 'page2'],
}
```

### Добавление блог-поста

Создайте файл в `blog/`:

```markdown
---
slug: название-поста
title: Заголовок поста
authors: [имя]
tags: [тег1, тег2]
---

Содержание поста...
```

## 🎨 Кастомизация

### Изменение темы

Отредактируйте `src/css/custom.css`:

```css
:root {
  --ifm-color-primary: #3B82F6;
  --ifm-color-primary-dark: #2563EB;
}
```

### Добавление компонента

Создайте компонент в `src/components/`:

```tsx
export default function MyComponent() {
  return <div>Hello!</div>;
}
```

Используйте в MDX:

```mdx
import MyComponent from '@site/src/components/MyComponent';

<MyComponent />
```

## 🔧 Конфигурация

Основные настройки в `docusaurus.config.ts`:

```typescript
{
  title: 'SiteBuilder',
  tagline: 'Конструктор сайтов на русском языке',
  url: 'https://docs.sitebuilder.ru',
  baseUrl: '/',
  // ...
}
```

## 📝 Markdown особенности

### Предупреждения

```markdown
:::note
Примечание
:::

:::tip
Совет
:::

:::warning
Предупреждение
:::

:::danger
Опасность
:::
```

### Вкладки

```markdown
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

<Tabs>
  <TabItem value="js" label="JavaScript">
    ```js
    console.log('Hello');
    ```
  </TabItem>
  <TabItem value="ts" label="TypeScript">
    ```ts
    console.log('Hello');
    ```
  </TabItem>
</Tabs>
```

### Код с подсветкой

````markdown
```js title="app.js" {1,3-5}
function hello() {
  console.log('Hello!');
  return true;
}
```
````

## 🌍 Интернационализация

Для добавления других языков:

1. Обновите `docusaurus.config.ts`:

```typescript
i18n: {
  defaultLocale: 'ru',
  locales: ['ru', 'en'],
},
```

2. Создайте переводы в `i18n/`:

```
i18n/
├── ru/
│   └── docusaurus-plugin-content-docs/
└── en/
    └── docusaurus-plugin-content-docs/
```

## 📊 Аналитика

Добавьте Google Analytics в `docusaurus.config.ts`:

```typescript
gtag: {
  trackingID: 'G-XXXXXXXXXX',
},
```

## 🔍 Поиск

Docusaurus поддерживает несколько поисковых решений:
- [Algolia DocSearch](https://docsearch.algolia.com/)
- Локальный поиск

## 🤝 Вклад в документацию

1. Форкните репозиторий
2. Создайте ветку (`git checkout -b docs/improvement`)
3. Внесите изменения
4. Закоммитьте (`git commit -m 'Улучшена документация'`)
5. Отправьте изменения (`git push origin docs/improvement`)
6. Создайте Pull Request

## 📚 Полезные ссылки

- [Официальная документация Docusaurus](https://docusaurus.io/)
- [Markdown Guide](https://www.markdownguide.org/)
- [MDX Documentation](https://mdxjs.com/)

---

**Вопросы?** Создайте Issue в репозитории!
