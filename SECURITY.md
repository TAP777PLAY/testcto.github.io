# 🔒 Политика безопасности

## Поддерживаемые версии

В настоящее время поддерживается:

| Версия | Поддерживается          |
| ------ | ----------------------- |
| 0.1.x  | :white_check_mark:      |

## Сообщение об уязвимостях

Если вы обнаружили уязвимость в безопасности, пожалуйста:

1. **НЕ** создавайте публичный issue
2. Отправьте email на security@example.com (замените на реальный)
3. Опишите уязвимость максимально детально
4. Укажите шаги для воспроизведения
5. Предложите возможное решение (если есть)

Мы ответим в течение 48 часов и начнём работу над исправлением.

## Меры безопасности в проекте

### Аутентификация

- ✅ Пароли хэшируются с помощью bcryptjs
- ✅ JWT токены для сессий
- ✅ Secure cookies в production
- ✅ CSRF защита через NextAuth.js

### API Security

- ✅ Проверка авторизации на всех защищённых endpoints
- ✅ Валидация входных данных
- ✅ Защита от SQL инъекций (Prisma ORM)
- ⚠️ Rate limiting (рекомендуется настроить в production)

### База данных

- ✅ Параметризованные запросы через Prisma
- ✅ Connection pooling
- ⚠️ Backup стратегия (настраивается отдельно)
- ⚠️ Шифрование чувствительных данных (рекомендуется)

### Frontend

- ✅ XSS защита через React
- ✅ Content Security Policy (CSP) headers
- ✅ Secure dependencies (регулярные обновления)
- ⚠️ HTTPS only в production

## Рекомендации по безопасности

### Для разработчиков

1. **Никогда** не коммитьте `.env` файлы
2. Используйте сильные NEXTAUTH_SECRET (минимум 32 символа)
3. Регулярно обновляйте зависимости
4. Проверяйте npm audit перед деплоем
5. Используйте environment variables для секретов

### Для администраторов

1. **Базовые настройки:**
   ```bash
   # Генерация secure secret
   openssl rand -base64 32
   
   # Проверка уязвимостей
   npm audit
   
   # Обновление зависимостей
   npm update
   ```

2. **Database Security:**
   - Используйте сильные пароли
   - Ограничьте доступ по IP
   - Включите SSL/TLS
   - Настройте регулярные backup

3. **Vercel Security:**
   - Используйте Environment Variables для секретов
   - Включите Vercel Authentication
   - Настройте Custom Domain с HTTPS
   - Активируйте Vercel Firewall

4. **Rate Limiting:**
   ```typescript
   // Пример middleware для rate limiting
   import rateLimit from 'express-rate-limit';
   
   const limiter = rateLimit({
     windowMs: 15 * 60 * 1000, // 15 минут
     max: 100 // макс 100 запросов
   });
   ```

## Безопасная конфигурация

### Production Environment Variables

```env
# НИКОГДА не используйте эти значения в production!

# Генерируйте уникальные ключи:
NEXTAUTH_SECRET=$(openssl rand -base64 32)

# Используйте HTTPS:
NEXTAUTH_URL=https://yourdomain.com

# Secure Database:
DATABASE_URL=postgresql://user:strongpassword@host/db?sslmode=require
```

### Headers Security

Next.js автоматически добавляет безопасные headers. Можно расширить:

```typescript
// next.config.js
module.exports = {
  async headers() {
    return [
      {
        source: '/(.*)',
        headers: [
          {
            key: 'X-Frame-Options',
            value: 'DENY'
          },
          {
            key: 'X-Content-Type-Options',
            value: 'nosniff'
          },
          {
            key: 'Referrer-Policy',
            value: 'strict-origin-when-cross-origin'
          }
        ]
      }
    ];
  }
};
```

## Чек-лист безопасности

Перед деплоем в production:

- [ ] NEXTAUTH_SECRET установлен и уникален
- [ ] DATABASE_URL содержит сильный пароль
- [ ] HTTPS настроен и принудителен
- [ ] Переменные окружения не в Git
- [ ] npm audit показывает 0 уязвимостей
- [ ] Rate limiting настроен
- [ ] Backup базы данных настроен
- [ ] CORS настроен корректно
- [ ] CSP headers настроены
- [ ] Логирование безопасности включено

## Известные ограничения

- Rate limiting требует дополнительной настройки
- Нет встроенного 2FA (планируется)
- Email верификация не реализована (планируется)

## Обновления безопасности

Подпишитесь на уведомления GitHub для получения обновлений безопасности.

```bash
# Проверка уязвимостей
npm audit

# Автоматическое исправление
npm audit fix

# Принудительное исправление (осторожно!)
npm audit fix --force
```

## Ресурсы

- [OWASP Top 10](https://owasp.org/www-project-top-ten/)
- [Next.js Security](https://nextjs.org/docs/advanced-features/security-headers)
- [Vercel Security](https://vercel.com/docs/security)
- [Prisma Security](https://www.prisma.io/docs/concepts/components/prisma-client/security)

## Благодарности

Мы благодарим всех исследователей безопасности, которые помогают делать проект безопаснее.

---

Последнее обновление: 2024
